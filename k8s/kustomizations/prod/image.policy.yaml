apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: offer-renderer
  namespace: offer-renderer
spec:
  filterTags:
    pattern: '^(?P<ver>v?\d+\.\d+\.\d+)(?P<comment>.*)-prod$'
    extract: '$ver'
  policy:
    semver:
      range: '>=0.0.0'
